machine:
  python:
    version: 2.7.10
  node:
    version: 6.9.1
  services:
    - docker

dependencies:
  pre:
    - pip install ansible
    - pip install --upgrade setuptools

deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy.sh
